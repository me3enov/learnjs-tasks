'use strict';

const name = 'Ilya';

alert( `hello ${1}` ); // => alert message "hello 1"
alert( `hello ${'name'}` ); // => alert message "hello name"
alert( `hello ${name}` ); // => alert message "hello Ilya"
